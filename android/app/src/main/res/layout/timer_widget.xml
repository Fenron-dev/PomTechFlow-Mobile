<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp"
    android:gravity="center_vertical">

    <!-- Header Row: app name + open tasks badge -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_app_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="PomTechFlow"
            android:textSize="10sp"
            android:textColor="#99FFFFFF"
            android:textStyle="bold"
            android:letterSpacing="0.1" />

        <TextView
            android:id="@+id/tv_open_tasks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 offen"
            android:textSize="10sp"
            android:textColor="#99FFFFFF" />
    </LinearLayout>

    <!-- Chronometer: counts up when running -->
    <Chronometer
        android:id="@+id/chronometer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="32sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:fontFamily="monospace" />

    <!-- Task name -->
    <TextView
        android:id="@+id/tv_task_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="Kein aktiver Task"
        android:textSize="12sp"
        android:textColor="#CCFFFFFF"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Status row: icon + label + open button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Kein Timer"
            android:textSize="11sp"
            android:textColor="#88FFFFFF" />

        <TextView
            android:id="@+id/btn_open"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Öffnen ›"
            android:textSize="11sp"
            android:textColor="#80B9FF"
            android:padding="4dp" />
    </LinearLayout>

</LinearLayout>
